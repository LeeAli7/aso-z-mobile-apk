/**
 * Конфигурация приложения. URL бэкенда для синхрона — единственная
 * сторона, где используется наш сервер (только вход/профиль, не LLM).
 * LLM-трафик идёт напрямую к провайдерам (см. core/gateway.ts).
 */

export const config = {
  /**
   * Базовый URL нашего API для синхронизации с ТГ.
   * Поменяй на публичный адрес бэкенда.
   */
  apiBase: "http://127.0.0.1:8000",
} as const;